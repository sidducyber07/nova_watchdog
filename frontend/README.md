# Nova Watchdog Frontend

Quickstart:

```bash
cd frontend
npm install
npm run dev
```

This will run the Next.js app on http://localhost:3000 and expects the API to be available at http://localhost:8000 (CORS enabled).
